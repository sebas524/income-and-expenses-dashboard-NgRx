export class IncomeExpenses {
  // ?========================= Constructor =========================
  constructor(
    public description: string,
    public amount: number,
    public type: string,
    public id?: string
  ) {}
  // ?============================================================
}
