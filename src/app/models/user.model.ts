export class User {
  // ?========================= Constructor =========================
  constructor(public uid: string, public name: string, public email: string) {}
  // ?============================================================
}
