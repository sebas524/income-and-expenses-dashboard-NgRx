import { OrderIncomeExpensesPipe } from './order-income-expenses.pipe';

describe('OrderIncomeExpensesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderIncomeExpensesPipe();
    expect(pipe).toBeTruthy();
  });
});
